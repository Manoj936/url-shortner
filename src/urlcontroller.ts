import { Request, Response } from "express";
const dotenv = require('dotenv')
dotenv.config();


export const shorten_url = async (req: Request, res: Response) => {
  try {
    
  } catch (e) {

  }
};

export const redirect_url = async(req : Request ,  res:Response)=>{

}
